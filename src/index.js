//Esse arquivo é o ponto de entrada da nossa aplicação, o primeiro arquivo a ser executado que vai chamando outros.
import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);